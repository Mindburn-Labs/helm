# OSS-EXECUTOR Scope
Runs effects if and only if valid, notarized decision AND execution intent exist. Creates signed receipts. Validates output schemas. Enforces idempotency. Fail-closed receipt policy.
