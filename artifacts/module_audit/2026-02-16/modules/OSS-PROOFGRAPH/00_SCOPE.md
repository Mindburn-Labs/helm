# OSS-PROOFGRAPH Scope
Cryptographic DAG recording all decisions, effects, and denials. Hash-chained nodes with Lamport clock. JCS (RFC 8785) canonicalization for deterministic hashing. Chain validation. Signed nodes.
